apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: messenger

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres-pvc.yaml
  - postgres.yaml
  - migrate-users-job.yaml
  - migrate-messages-job.yaml
  - user-service.yaml
  - message-service.yaml
  - bff.yaml
  - frontend.yaml
  - storage.yaml   # StorageClass csi-s3 (cluster-scoped) + PVC message-uploads-pvc
  - minio.yaml     # MinIO Deployment + Service
